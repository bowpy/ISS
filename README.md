# ISS

International Space Station live information pull from open-notify API.

Python script which uses http://api.open-notify.org/astros.json to request live data about the ISS.

Reads the data with urllib and json then outputs it.
